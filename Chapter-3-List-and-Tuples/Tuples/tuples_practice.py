# WAP to count the number of students with the "A" grade in the following tuple
grade = ("C", "D", "A", "A", "B", "B", "A")
print(grade.count("A"))


# Store the above values in a list abd sorted them from "A" to "D"
grade = ["C", "D", "A", "A", "B", "B", "A"]
grade.sort()
print(grade)